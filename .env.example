# ==============================================
# CONFIGURACIÓN DE PRODUCCIÓN (DOCKER)
# ==============================================
# Este archivo es utilizado ÚNICAMENTE por 'docker-compose-prod.yml'.
# Modifica esto cuando quieras desplegar en un servidor real.
#
# NOTA: Si estás ejecutando localmente con 'npm run dev', 
# se usan los archivos .env dentro de las carpetas /client y /server.

# Server Port (Puerto interno del contenedor)
PORT=3001

# Client API URL
# En local: http://localhost:3001/api
# En producción: http://ITU_IP_PUBLICA:3001/api
VITE_API_URL=http://localhost:3001/api
